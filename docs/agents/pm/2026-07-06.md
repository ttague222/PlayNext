# PlayNxt PM Report — 2026-07-06 (Monday)

## PRD Compliance

**User Flow** — Implemented. Screens cover the full MVP flow: Welcome → Time → Mood → OptionalFilters → Results, plus GameDetail, History, Profile. Reroll (`/recommend/reroll` with `excluded_game_ids`), "I'll play this" acceptance, and "Played before" (`AlreadyPlayedModal`, `HistoryFeedbackModal`) all present. *Gap:* verify max-3 cap is enforced UI-side; `settings.max_recommendations` drives it server-side.

**Recommendation Engine** — Strong match to spec. `_filter_games` implements the exact fallback hierarchy (platform → genre/play_style → time → partial). `_score_games` matches Architecture heuristics (stop-friendliness +0.25/0.15, time-to-fun +0.2/0.1, mood +0.2, genre +0.15, platform +0.1, subscription +0.1). 7-day deprioritization (`_get_recently_shown`) and franchise diversity are implemented. *Beyond PRD:* `_apply_surprise_boost` deprioritizes AAA franchises to surface indies — good, but undocumented in PRD §4.2 Discovery toggle.

**Game Catalog** — Firestore-backed via `game_service`, admin CRUD in web-admin (`GameCatalog`, `GameEditor`). *Cannot confirm* the 200–300 MVP catalog size from code; recommend pulling `/api/games/stats`.

**User Preference Signals** — Implemented. `signal_service` + `/signals/*` routes cover feedback, accept, session, history. Signal types match schema (worked / not_good_fit / played_* / skipped / accepted). Anonymous session tracking present. Aligns with PRD §8.

## Next 3 Priorities

1. **Sync PRD + ARCHITECTURE with shipped monetization** *(small)* — Ads, premium, purchases, buckets, and affiliate links are live but PRD §11 still lists them as out-of-scope. Docs now actively contradict the product. Highest-leverage cleanup.
2. **Confirm catalog depth & subscription coverage** *(small–medium)* — MVP quality hinges on 200–300 curated games with accurate `subscription_services`. Audit real counts before pushing acquisition.
3. **Instrument success metrics (acceptance rate, rerolls/session, time-to-decision)** *(medium)* — PRD §12 targets exist but there's no evidence they're measured. Without this, engine tuning is blind. Wire the existing signals into the web-admin `Analytics` page.

## Growth Opportunities

- **Bucket/save feature is a monetization wedge** — `SaveToBucketModal` + `routes_buckets` already exist. Position saved lists as the Phase 2 "Smart History" premium hook (curated re-recommendations from your buckets).
- **"Why not?" for skipped games** — PRD Phase 2 feature; the skip signal is already captured. Low-effort negative-signal loop that improves match quality.
- **Time-to-decision as a headline metric** — reinforces the core value prop (stop deciding, start playing) and is a compelling marketing stat.
- **Remote Config-driven experiments** — `RemoteConfigService` is in place; use it to A/B test scoring weights against acceptance rate.

## Doc Health

- **ARCHITECTURE.md is stale.** Repo structure omits `routes_buckets`, `routes_config`, `bucket_service`, and the mobile monetization layer (Ad/Premium contexts, purchase/affiliate services). It lists **Pinecone** as a data store, but `api-service/src/db/` contains only `firebase.py` — no vector client. `docs/` now also holds `GETTING_STARTED.md` and `MONETIZATION.md`, unlisted in the tree.
- **PRD.md is partially outdated.** §11 (Out of Scope) still excludes ads, payments, subscriptions, and wishlist/save — all now shipped. Update to reflect the Phase 2 pivot, or split a v2 PRD.

## Weekly Summary (Monday)

No commits in the past week. Last activity was `88d0b8a Updates` on **2026-02-19**; the repo has been quiet ~4.5 months. The 1.0.6 App Store submission (`f2bb42c`) and the platform-filter/indie-boost fix (`dd391dc`) were the last substantive changes, both late January. **Health:** codebase is feature-complete against MVP and has moved into monetization, but momentum has stalled and documentation has drifted behind the code. Recommend a doc-sync pass and a metrics-instrumentation sprint to re-establish direction.

*Suggestions only — not self-approved. No source code modified.*
